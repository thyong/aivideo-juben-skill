# AI 视频剧本助手：安装与使用

这套能力可以让支持本地文件和终端操作的 ChatGPT/Codex 客户端按本文档代为安装。普通网页聊天无法直接安装电脑上的 Skills。

## 一、交给客户端自动安装

先确保你已经获得私有仓库 `thyong/aivideo-juben-skill` 的访问权限，然后在 ChatGPT/Codex 客户端中发送：

```text
请将 git@github.com:thyong/aivideo-juben-skill.git 克隆到我的 ~/.codex/skill-repos/aivideo-juben-skill，
完整阅读仓库中的《同事安装使用说明.md》，安装全部 Skills 并运行部署自检。
不要修改任何业务规则。完成后告诉我安装和自检结果。
```

如果客户端无法访问私有仓库，先在电脑上配置 GitHub SSH 权限，或由管理员将仓库文件夹交付给你。

## 二、手动安装

```bash
mkdir -p "$HOME/.codex/skill-repos"
git clone git@github.com:thyong/aivideo-juben-skill.git "$HOME/.codex/skill-repos/aivideo-juben-skill"
"$HOME/.codex/skill-repos/aivideo-juben-skill/install.sh"
"$HOME/.codex/skill-repos/aivideo-juben-skill/doctor.sh"
```

看到 `Skill deployment check passed.` 表示安装成功。

## 三、开始使用

1. 在 ChatGPT/Codex 客户端中打开克隆后的 `aivideo-juben-skill` 文件夹，并将其作为一个专用项目。
2. 安装或升级后新建一个任务。
3. 输入 `开始`，按菜单选择功能。
4. 也可以直接输入 `海洋风转换`、`剧本多版本`、`剧本二创`、`动物人转换` 等命令。

原剧本可以直接作为 `.txt` 文件添加到任务中。处理后的文件默认保存在原文件附近。

## 四、升级

在客户端中发送：

```text
请阅读 AI 视频剧本助手项目的《同事安装使用说明.md》，执行日常升级和部署自检，不要修改业务规则。
```

也可以手动执行：

```bash
"$HOME/.codex/skill-repos/aivideo-juben-skill/update.sh"
```

升级完成后新建一个任务，使用刷新后的 Skills。

## 五、遇到问题

- `Permission denied` 或无法克隆：检查 GitHub 仓库权限和 SSH 登录。
- 安装提示目标已存在：让客户端先判断它是否为旧软链接；只有确认为旧软链接时，才执行 `install.sh --repair`。
- 菜单没有出现新功能：先运行 `doctor.sh`，然后新建任务。
